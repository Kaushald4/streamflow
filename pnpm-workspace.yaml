packages: []

allowBuilds:
  sharp: false
  unrs-resolver: false
  # stream-resolver's `prepare` compiles dist/ during install; dist is not
  # committed, so without this the install fails with
  # ERR_PNPM_GIT_DEP_PREPARE_NOT_ALLOWED.
  stream-resolver: true
